block container
    div(class='row', style='margin-top:2%;')
        div(class='col-xs-6', style=' text-align:left; padding-left:2%, margin-top:5%;')
            button(class='btn btn-lg btn-default', type='submit', onclick='logout()') Log Out
        div(class='col-xs-6', style=' text-align:right; padding-right:2%')
            h3(style='text-align:right; display:inline' id='highscore') High Score: #{highscore}
    div(class='row', style='margin-top:15%;')
        div(class='col-xs-6', style=' text-align:left; padding-left:20%')
            button(class='btn btn-lg btn-default', type='submit', style='padding: 4% 20% 4%; font-size:200%', onclick='findGame()') P VS P 
            //this button will first check if there is a table row in room, in the same game mode as the one selected that hasnt started or ended, if there isnt it will add a new one and take the user to th searching page, 
            //if there is a available game it will go to the countdown page and will beguin the countdown for the game to start.
            h2
                span(class='glyphicon glyphicon-user', style='padding-left:14%; padding-right:14%')
                p(style='display:inline')      
                span(class='glyphicon glyphicon-user', style='padding-left:5%')
        div(class='col-xs-6', style='text-align:right; padding-right:20%')
            button(class='btn btn-lg btn-default', type='submit', style='padding: 4% 20% 4%; font-size:200%', onclick='findComputerGame()') P VS C
            //Once this button is clicked the countdown will begin
            h2 
                span(class='glyphicon glyphicon-user',style='padding-left:10%; padding-right:14%')
                p(style='display:inline')      
                span(class='glyphicon glyphicon-open', style='padding-left:5%; padding-right:2%;')
    div(class='row', style='text-align:center; margin-top:7%;')
        select(class="selectpicker", style='font-size:200%;', id='modeChooser')
            option Classic
            option Same Meanings
            option Gerneric Terms
            option Opposites
    div(class='row', style='text-align:center; margin-top:10%;')
        button(class='btn btn-lg btn-default', type='submit', style='padding:2% 6% 2%; font-size:150%', onclick='highScoreTable(true)') View High scores
    div(class='row', style='text-align:right; margin-top:1%; margin-right:3%;')
        button(class='btn btn-lg btn-default', type='submit', style='padding:3% 6% 3% 6%; font-size:150%', onclick='loadPage("rules")') ?
